# Capture summaries

Commit Markdown summaries using [the template](../templates/capture-summary.md).
Large .wpix and .rdc files stay local and are ignored. Captures may depend on specific
hardware/runtime versions; record the environment rather than assuming portability.